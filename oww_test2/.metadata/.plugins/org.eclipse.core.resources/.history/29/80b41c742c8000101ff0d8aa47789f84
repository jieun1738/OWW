<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Own Wedding Wallet</title>
<link rel="stylesheet" href="/css/page.css" />
<link rel="stylesheet" href="/css/design.css" />
<link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
<style>
/* 핑크/보라 그라데이션 테마 */
body {
    display: block;
    background: linear-gradient(135deg, #f8d7da 0%, #e1bee7 50%, #c8a2c8 100%);
    font-family: 'Noto Sans KR', sans-serif;
    min-height: 100vh;
}

.container {
    max-width: 1200px;
    margin: 50px auto;
}

.service-cards {
    display: flex;
    flex-direction: column;
    gap: 45px;
    padding: 20px;
}

.service-card {
    display: flex;
    justify-content: space-between;
    align-items: center;
    background: rgba(255, 255, 255, 0.95);
    border-radius: 25px;
    padding: 36px 48px;
    min-height: 180px;
    box-shadow: 0 10px 30px rgba(200, 162, 200, 0.35);
    cursor: pointer;
    transition: all 0.3s ease;
    backdrop-filter: blur(12px);
    border: 1px solid rgba(255, 255, 255, 0.25);
}

.service-card:hover {
    transform: translateY(-6px);
    box-shadow: 0 15px 40px rgba(200, 162, 200, 0.45);
}

.service-card-left h1 {
    font-size: 22px;
    font-weight: 600;
    margin: 4px 0;
}

.service-card-left h1 strong {
    color: #6b4c6b;
    font-size: 28px;
    font-weight: 700;
}

.service-card-left h2 {
    color: #8b5a8c;
    font-size: 20px;
    font-weight: 600;
    margin: 6px 0;
}

.balance-text {
    color: #a569a5;
    font-size: 16px;
}

.balance-amount {
    color: #6b4c6b;
    font-weight: 700;
    font-size: 18px;
}

.account-number, .safebox-number {
    color: #8b5a8c;
    font-family: 'Courier New', monospace;
    font-weight: 700;
    font-size: 18px;
}

.no-account h2, .no-safebox h2 {
    color: #6b4c6b;
    font-weight: 700;
    font-size: 20px;
}

.no-account p, .no-safebox p {
    color: #a569a5;
    font-size: 16px;
}

.service-card-right {
    display: flex;
    flex-direction: column;
    gap: 12px;
    min-width: 140px;
}

.service-card-right>* {
    width: 140px;
}

.banking-btn {
    padding: 14px 30px;
    border-radius: 28px;
    background: linear-gradient(135deg, #c8a2c8 0%, #a569a5 100%);
    color: white;
    font-weight: 600;
    text-decoration: none;
    border: none;
    cursor: pointer;
    transition: all 0.3s ease;
    box-shadow: 0 5px 18px rgba(165, 105, 165, 0.35);
    text-align: center;
}

.banking-btn:hover {
    background: linear-gradient(135deg, #a569a5 0%, #8b5a8c 100%);
    transform: translateY(-2px);
    box-shadow: 0 6px 22px rgba(139, 90, 140, 0.45);
}

/* 반응형 */
@media (max-width: 768px) {
    .service-card {
        flex-direction: column;
        gap: 24px;
        text-align: center;
        padding: 28px 20px;
    }
    .service-card-right {
        width: 100%;
        flex-direction: row;
        justify-content: center;
        gap: 12px;
    }
    .banking-btn {
        flex: 1;
        padding: 12px 20px;
    }
}
</style>
</head>
<body class="site-layout">

<!-- header fragment 포함 -->
<div th:replace="~{header :: siteHeader('banking')}"></div>

<!-- nav fragment 포함 -->
<nav th:replace="~{nav :: siteNav('banking')}"></nav>

<div class="container">
    <section class="service-cards">
        <!-- 계좌 카드 -->
        <article class="service-card" tabindex="0">
            <div class="service-card-left">
                <div class="user-greeting">
                    <h1>
                        <strong th:text="${userName ?: '고객'}">고객</strong>님의 통장
                    </h1>
                </div>
                <div th:if="${hasAccount}" class="account-info">
                    <h2 class="account-number" th:text="${accountNumber}">110-000000-001</h2>
                    <div class="balance-text">
                        잔액: <span class="balance-amount">₩<span th:text="${#numbers.formatInteger(balance, 3, 'COMMA')}">0</span></span>
                    </div>
                </div>
                <div th:unless="${hasAccount}" class="no-account">
                    <h2>계좌가 없습니다</h2>
                    <p>Own Wedding Wallet 계좌를 생성해주세요</p>
                </div>
            </div>
            <div class="service-card-right">
                <a href="/banking/transfer_1" class="banking-btn">이체하기</a>
                <a href="/banking/history" class="banking-btn">내역조회</a>
            </div>
        </article>

        <!-- 세이프박스 카드 -->
        <article class="service-card" tabindex="0">
            <div class="service-card-left">
                <div class="user-greeting">
                    <h1>
                        <strong th:text="${userName ?: '고객'}">고객</strong>님의 세이프박스
                    </h1>
                </div>
                <div th:if="${hasSafebox}" class="safebox-info">
                    <h2 class="safebox-number" th:text="${safeboxNumber}">SB-000000-001</h2>
                    <div class="balance-text">
                        잔액: <span class="balance-amount">₩<span th:text="${#numbers.formatInteger(safeboxBalance, 3, 'COMMA')}">0</span></span>
                    </div>
                </div>
                <div th:unless="${hasSafebox}" class="no-safebox">
                    <h2>세이프박스가 없습니다</h2>
                    <p>Own Wedding Wallet 세이프박스를 개설해주세요</p>
                </div>
            </div>
            <div class="service-card-right">
                <a href="/banking/safebox/main" class="banking-btn">세이프박스</a>
                <a href="/safebox/history" class="banking-btn auth-btn" data-url="/api/check-jwt">내역조회</a>
            </div>
        </article>
    </section>
</div>

<!-- footer fragment 포함 -->
<div th:replace="~{footer :: siteFooter}"></div>

</body>
</html>
