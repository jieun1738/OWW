<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Own Wedding Wallet</title>
<link rel="stylesheet" href="/css/page.css" />
<link rel="stylesheet" href="/css/design.css" />
<link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
<script src="https://cdn.jsdelivr.net/npm/tsparticles@2.12.0/tsparticles.bundle.min.js"></script>
<style>
/* page.css 오버라이드 */
body {
	display: block !important;
	justify-content: initial !important;
	align-items: initial !important;
}

.container {
	max-width: 1200px;
	margin: 50px auto;
}

.service-cards {
	display: flex;
	flex-direction: column;
	gap: 40px;
	padding: 20px;
}

.service-card {
	display: flex;
	justify-content: space-between;
	align-items: center;
	background: #fff;
	border-radius: 20px;
	padding: 24px 32px;
	box-shadow: 0 6px 15px rgba(0, 0, 0, 0.07);
	cursor: pointer;
	transition: transform 0.2s ease;
}

.service-card:hover {
	transform: scale(1.02);
}

.service-card-left h1, .service-card-left h2, .service-card-left h3 {
	margin: 4px 0;
}

.service-card-right {
	display: flex;
	flex-direction: column;
	gap: 10px;
}

.history-btn {
	padding: 12px 24px;
	border-radius: 20px;
	border: 2px solid #ff726f;
	background: transparent;
	color: #ff726f;
	font-weight: 600;
	cursor: pointer;
	transition: all 0.3s ease;
}

.history-btn:hover {
	background: #ff726f;
	color: #fff;
	border-color: transparent;
}
</style>
</head>
<body class="site-layout">

	<!-- header fragment 포함 -->
	<div th:replace="~{header :: siteHeader('banking')}"></div>
	
	<!-- nav fragment 포함 -->  
	<nav th:replace="~{nav :: siteNav('banking')}"></nav>

	<div class="container">
		<section class="service-cards">
			<article class="service-card" tabindex="0">
				<div class="service-card-left">
					<div class="user-greeting">
						<h1>
							<strong th:text="${userName ?: '고객'}">고객</strong>님의 통장
						</h1>
					</div>

					<div th:if="${hasAccount}" class="account-info">
						<h2 class="account-number" th:text="${accountNumber}">110-000000-001</h2>
						<div class="balance-text">
							잔액: <span class="balance-amount">₩<span
								th:text="${#numbers.formatInteger(balance, 3, 'COMMA')}">0</span></span>
						</div>
					</div>

					<div th:unless="${hasAccount}" class="no-account">
						<h2>계좌가 없습니다</h2>
						<p>Own Wedding Wallet 계좌를 생성해주세요</p>
					</div>
				</div>

				<div class="service-card-right">
					<a href="/banking/transfer_1" class="banking-btn">이체하기</a>
					<a href="/banking/history" class="banking-btn">내역조회</a>
				</div>
			</article>

			<article class="service-card" tabindex="0">
				<div class="service-card-left">
					<div class="user-greeting">
						<h1>
							<strong th:text="${userName ?: '고객'}">고객</strong>님의 세이프박스
						</h1>
					</div>

					<div th:if="${hasSafebox}" class="safebox-info">
						<h2 class="safebox-number" th:text="${safeboxNumber}">SB-000000-001</h2>
						<div class="balance-text">
							잔액: <span class="balance-amount">₩<span
								th:text="${#numbers.formatInteger(safeboxBalance, 3, 'COMMA')}">0</span></span>
						</div>
					</div>

					<div th:unless="${hasSafebox}" class="no-safebox">
						<h2>세이프박스가 없습니다</h2>
						<p>Own Wedding Wallet 세이프박스를 개설해주세요</p>
					</div>
				</div>

				<div class="service-card-right">
					<a href="/banking/safebox/main" class="banking-btn">세이프박스</a>
					<button class="history-btn auth-btn" data-url="/api/check-jwt"
						data-redirect="/safebox/history">내역조회</button>
				</div>
			</article>
		</section>
	</div>

	<!-- footer fragment 포함 -->
	<div th:replace="~{footer :: siteFooter}"></div>

</body>
</html>