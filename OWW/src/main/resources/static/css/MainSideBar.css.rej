diff a/OWW/src/main/resources/static/css/MainSideBar.css b/OWW/src/main/resources/static/css/MainSideBar.css	(rejected hunks)
@@ -181,44 +181,87 @@
 .icon-box i {
 	color: var(--ink);
 }
-/*마이페이지에서 사용하는 부분*/
-.budget {
-	display: flex;
-	flex-direction: column;
-	justify-content: center;
-	margin: 5px 30px;
-	width:70%;
-	height: 200px;
-	gap:2px;
-	background: var(--brand-weak);
-	border: 1px solid var(--line);
-	border-radius: var(--r);
-	box-shadow: var(--shadow);
-	padding: 5px;
-	color: var(--ink);
+
+/* ===============================
+   LeftNav 버튼 스타일 보강 (드롭인)
+   기존 .ln-item 구조 그대로 사용
+   =============================== */
+
+.ln-list {
+  display: grid;
+  grid-template-columns: 1fr;
+  gap: 10px;
+}
+
+/* 기본 버튼 */
+.ln-item {
+  position: relative;
+  display: inline-flex;
+  align-items: center;
+  gap: 10px;
+  padding: 12px 14px;
+  border-radius: 14px;
+  border: 1px solid var(--line);
+  background: var(--card);
+  color: var(--ink);
+  text-decoration: none;
+  box-shadow: 0 2px 8px rgba(17,24,39,.04);
+  transition: background .18s ease, border-color .18s ease, box-shadow .18s ease, transform .06s ease;
+  line-height: 1;
+  min-height: 42px;
 }
-.budget h5{
-	text-align: center;
-	
-	font-size: 15px;
-	color: var(--brand);
+
+.ln-item i.bx {
+  font-size: 18px;
+  opacity: .9;
+  transition: transform .12s ease, opacity .12s ease;
 }
-.budget h6{
-	text-align: center;
-	font-size: 14px;
+
+/* hover */
+.ln-item:hover {
+  border-color: #d1d5db;
+  background: #fafafa;
+  box-shadow: 0 4px 14px rgba(17,24,39,.06);
+  transform: translateY(-1px);
 }
-.budget .mini-list{
-	width:90%;
-	text-align: left;
-	font-size: 12px;
-	
+.ln-item:hover i.bx { opacity: 1; }
+
+/* active 상태 (선택됨) */
+.ln-item.active {
+  background: var(--brand);
+  border-color: var(--brand);
+  color: #fff;
+  box-shadow: 0 6px 18px rgba(124,58,237,.25);
 }
-.budget h4,
-.budget h5,
-.budget h6 {
-  margin: 2px 0;   /* 위아래 여백 줄임 */
-  /* 필요하면 굵기도 조정 */
+.ln-item.active i.bx { color: #fff; }
+
+/* 키보드 포커스 접근성 */
+.ln-item:focus-visible {
+  outline: 3px solid rgba(124,58,237,.35);
+  outline-offset: 2px;
 }
 
+/* 텍스트 줄바꿈 방지 & 말줄임 (라벨이 길 때) */
+.ln-item span {
+  white-space: nowrap;
+  overflow: hidden;
+  text-overflow: ellipsis;
+}
 
+/* 사이드 “활성 표시 바” (시각 강조, 필요 없으면 주석) */
+.ln-item.active::before {
+  content: "";
+  position: absolute;
+  inset: 6px auto 6px 6px;
+  width: 4px;
+  border-radius: 2px;
+  background: rgba(255,255,255,.9);
+}
 
+/* 작은 화면 살짝 컴팩트 */
+@media (max-width: 1200px) {
+  .ln-item { padding: 11px 12px; min-height: 40px; }
+}
+@media (max-width: 860px) {
+  .ln-item { padding: 10px 12px; border-radius: 12px; }
+}
